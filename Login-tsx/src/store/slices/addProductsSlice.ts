import { createSlice } from "@reduxjs/toolkit";

type initialStateType={

}

const initialState = {

}

const addProductsSlice = createSlice({
    name : 'addProducts',
    initialState,
    reducers : {
        addProducts(state){
            
        }
    }
})